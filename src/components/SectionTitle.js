import React from 'react';

const SectionTitle = () => {
    return (
        <div className='section-title-wrapper flex flex-col justify-center items-center'>
            <div className='title'>Our specialisation</div>
            <div className='sub-title'>
                We work across all industry sectors, specialising in those
                listed below. Don't see your industry here? Don't worry - we can
                still help
            </div>
        </div>
    );
};

export default SectionTitle;
